@model CriticalShop.Models.Admin

@{
    ViewData["Title"] = "Excluir Administrador";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h2>Excluir Administrador</h2>
                <a asp-action="Admins" class="btn btn-secondary">Voltar</a>
            </div>

            <div class="alert alert-warning">
                <h4>Atenção!</h4>
                <p>Tem certeza que deseja excluir este administrador?</p>
            </div>

            <div class="card">
                <div class="card-body">
                    <dl class="row">
                        <dt class="col-sm-3">ID</dt>
                        <dd class="col-sm-9">@Model.Id</dd>

                        <dt class="col-sm-3">Nome</dt>
                        <dd class="col-sm-9">@(Model.Nome ?? "Não informado")</dd>

                        <dt class="col-sm-3">Email</dt>
                        <dd class="col-sm-9">@Model.Email</dd>

                        <dt class="col-sm-3">Data de Cadastro</dt>
                        <dd class="col-sm-9">@Model.DataCadastro.ToString("dd/MM/yyyy HH:mm")</dd>

                        <dt class="col-sm-3">Tipo</dt>
                        <dd class="col-sm-9">
                            @if (Model.IsSuperAdmin)
                            {
                                <span class="badge bg-warning">Super Admin</span>
                            }
                            else
                            {
                                <span class="badge bg-info">Admin</span>
                            }
                        </dd>

                        <dt class="col-sm-3">Status</dt>
                        <dd class="col-sm-9">
                            @if (Model.Ativo)
                            {
                                <span class="badge bg-success">Ativo</span>
                            }
                            else
                            {
                                <span class="badge bg-danger">Inativo</span>
                            }
                        </dd>
                    </dl>

                    <form asp-action="ExcluirAdmin" method="post">
                        <input type="hidden" asp-for="Id" />
                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-danger">Confirmar Exclusão</button>
                            <a asp-action="Admins" class="btn btn-secondary">Cancelar</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
