@model IEnumerable<CriticalShop.Models.Usuario>

@{
    ViewData["Title"] = "Gerenciar Usuários";
}
<link rel="stylesheet" href="@Url.Content("~/css/crudProdutos/style.css")">
<div class="container mt-4">
    <div class="row">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h2>Gerenciar Usuários</h2>
                <div>
                    <a asp-action="Index" class="btn btn-secondary">Voltar ao Hub</a>
                </div>
            </div>

            @if (TempData["ErrorMessage"] != null)
            {
                <div class="alert alert-danger">@TempData["ErrorMessage"]</div>
            }
            @if (TempData["SuccessMessage"] != null)
            {
                <div class="alert alert-success">@TempData["SuccessMessage"]</div>
            }

            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Nome</th>
                            <th>Email</th>
                            <th>CPF</th>
                            <th>Telefone</th>
                            <th>Data Cadastro</th>
                            <th>Status</th>
                            <th>Ações</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var usuario in Model)
                        {
                            <tr>
                                <td>@usuario.Id</td>
                                <td>@usuario.Nome</td>
                                <td>@usuario.Email</td>
                                <td>@usuario.Cpf</td>
                                <td>@usuario.Telefone</td>
                                <td>@usuario.DataCadastro.ToString("dd/MM/yyyy")</td>
                                <td>
                                    @if (usuario.Ativo)
                                    {
                                        <span class="badge bg-success">Ativo</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-danger">Inativo</span>
                                    }
                                </td>
                                <td>
                                    <a asp-action="DetalhesUsuario" asp-route-id="@usuario.Id" class="btn btn-info btn-sm">Detalhes</a>
                                    <a asp-action="EditarUsuario" asp-route-id="@usuario.Id" class="btn btn-warning btn-sm">Editar</a>
                                    <a asp-action="ExcluirUsuario" asp-route-id="@usuario.Id" class="btn btn-danger btn-sm">Excluir</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
